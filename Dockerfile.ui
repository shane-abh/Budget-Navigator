# Frontend UI Dockerfile (Multi-stage build)
FROM node:20-alpine AS builder

# Set working directory
WORKDIR /app

# Accept build argument for VITE_API_URL
ARG VITE_API_URL=http://localhost:8000
ENV VITE_API_URL=$VITE_API_URL

# Copy package files (copy entire directory contents)
# Using JSON array format to handle space in directory name
COPY ["ui/Tax RAG/", "./"]

# Install dependencies
RUN npm ci

# Build the application
RUN npm run build

# Production stage - serve with nginx
FROM nginx:alpine

# Copy built files from builder stage
COPY --from=builder /app/dist /usr/share/nginx/html

# Copy nginx configuration
# For docker-compose: uses service name 'api'
# For standalone: replace 'api' with 'host.docker.internal' or actual host
COPY ["ui/Tax RAG/nginx.conf", "/etc/nginx/conf.d/default.conf"]

# Expose port
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]

