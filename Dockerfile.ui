# Frontend UI Dockerfile
# CHANGE 1: Use 'slim' (Debian) instead of 'alpine' to fix DNS/Network timeouts
FROM node:20-slim AS builder

WORKDIR /app

# Build arguments
ARG VITE_API_URL=http://localhost:8000
ENV VITE_API_URL=$VITE_API_URL

# 1. Copy package files
COPY ["ui/Tax RAG/package.json", "ui/Tax RAG/package-lock.json", "./"]

# 2. Install dependencies AND TypeScript in one go
# We use 'npm install' to be safe, and add typescript globally just in case.
RUN npm install --verbose --maxsockets=1 && npm install -g typescript

# 3. Copy the source code
COPY ["ui/Tax RAG/", "./"]

# 4. Build
RUN npm run build

# --- Production Stage ---
# It is safe to keep Nginx as Alpine, it just serves static files
FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY ["ui/Tax RAG/nginx.conf", "/etc/nginx/conf.d/default.conf"]
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]